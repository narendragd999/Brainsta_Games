Scooter Runner - Smooth build

This version improves:
- Smoother swipe and lane transitions using easing and target lane interpolation.
- Perspective road and obstacle scaling (third-person behind camera feel).
- Better synthesized engine/truck sounds (layered harmonics) and impact sound.

Open index.html in a modern browser.