/**
 * firebaseConfig.ts
 * Replace the placeholders below with your Firebase project's config.
 *
 * To enable Firebase:
 * 1. npm install firebase
 * 2. Create a Firebase project, enable Auth & Firestore.
 * 3. Copy your web config and paste below.
 *
 * This file is intentionally optional — the app runs without Firebase.
 */

export const firebaseConfig = {
  apiKey: "<YOUR_API_KEY>",
  authDomain: "<YOUR_AUTH_DOMAIN>",
  projectId: "<YOUR_PROJECT_ID>",
  storageBucket: "<YOUR_STORAGE_BUCKET>",
  messagingSenderId: "<YOUR_SENDER_ID>",
  appId: "<YOUR_APP_ID>",
};
